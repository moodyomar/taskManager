class Task {
     constructor(_parent, taskName, taskTime, id) {
          this.parent = _parent;
          this.taskName = taskName;
          this.taskTime = taskTime;
          this.id = id;
     }
     //addToHtml
     //     renderHtml() {}
}